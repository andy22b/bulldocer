'-- M5FNDECL.BAS
'-- DECLARES for M5SUB.BAS AND M5MTSUB.BAS
' by Peter Zwick Dec, 1990
' last update Mar,1993

DECLARE sub inkey (d$)
DECLARE sub shellcom()
DECLARE sub inlet (kwait%, a$, klen%, kval%, kascii%)
DECLARE sub setscreen(k%)
'DECLARE sub setcolor (kcolor%)
DECLARE sub scline1 (ix%, iy%, kcolor%, kbox%)
DECLARE sub scline2 (ix1%, iy1%, ix2%, iy2%, kcolor%, kbox%)
DECLARE sub scdash2 (ix1%, iy1%, ix2%, iy2%, kcolor%, kbox%)
DECLARE sub sccircle (ix%, iy%, irad%, kcolor%)
DECLARE sub scclear()
DECLARE sub cline (irow%, icol%)
'DECLARE sub cnchar (irow%, icol%, n%)
DECLARE sub scpoint(ix%,iy%,kcolor%)
DECLARE sub sclocate(irow%,icol%)
DECLARE sub scprint(irow%,icol%,text$)
DECLARE sub scprintv(irow%,icol%,text$)
DECLARE sub scprintb(irow%,icol%,text$)
DECLARE sub scprintc(irow%,icol%,text$)
DECLARE sub scprintvfu(irow%,icol%,textuse$,a!)
DECLARE sub scprompt(irow%,icol%,text$)
DECLARE sub schit(a$)
DECLARE sub sccenter(a$,n%)
DECLARE sub scpause (irow%,jcol%)
DECLARE sub scbox(ntop%,nbottom%,ndprgnm%,top$,bottom$)
DECLARE sub notalone()
DECLARE sub welcome()

'-- DECLARES for M5FNOPEN subroutines
'DECLARE SUB parsefilenm (inputname$, fldrv$, flpth$, flnm$,flext$)
DECLARE SUB sort (numitem%, listitem$())
'DECLARE SUB buildname(openfilename$,drv$,filepath$,nm$,fex$,dfltfex$)
'DECLARE SUB getflnm (oldname$)
DECLARE FUNCTION GetCurDrv$()
DECLARE FUNCTION displaymsg$ (msgin$, okans$, longans%)
DECLARE FUNCTION displayerr$ (ierrnum%, openfilename$, igetans%)
DECLARE FUNCTION IChkDrv% (drv$, openfilename$, isoknmchg%)   'returns true if drive is removable
DECLARE FUNCTION listselect% (listcnt%, listitem$(), topmsg$, sorted%)
DECLARE FUNCTION isfile% (flnm$, fullflnm$, isoknmchg%)
DECLARE FUNCTION ifilefind% (flnm$, nextfile%, fullflnm$, isoknmchg%)
DECLARE FUNCTION ifileselect% (openfilename$, isoknmchg%)
DECLARE FUNCTION ifopen% (openmode$, numopenfile%, openfilename$, lenopenrec%, isoknmchg%)

DECLARE function igetyesno%(irow%,icol%,prompt$,dflt$,isokescape%) 'gets yes or no, allows esc, erases when done
DECLARE sub endcheck()
DECLARE function keepold% (flnm$)  'true if user wants to keep old file
DECLARE sub chkbound (ista%)
DECLARE sub m5synio (io%, idat%, ista%, nin%, n1%, a!(1), amax!, asq!)
DECLARE sub m5esio (io%, ista%, nph%, nin%, a!(1))
DECLARE sub m5tmpio (io%, ista%, nph%, nin%, a!(1), files$)
DECLARE sub memcheck()

DECLARE function keyget% (b$, dtype$,inson%)
DECLARE FUNCTION iget% (irow%, icol%, kwait%, a$, dtype$, n%)
DECLARE function istype%(a$,dtype$)
DECLARE function input1% (irow%,icol%,prompt$,a$,kwait%,kokesc%,kerase%,okans$)
DECLARE function inputn% (irow%,icol%,prompt$,a$,kwait%,kokesc%,kerase%,dtype$,n%)
DECLARE function alltrim$(a$)
DECLARE function numlist$(nl%,nu%)
DECLARE sub settxtcol()
DECLARE sub setvarcol()
'DECLARE sub setboxcol()
DECLARE sub setchccol()
DECLARE sub setmsgcol()
'DECLARE sub sethilcol()
'DECLARE sub wiggle(kbeginx%,krow%,kfinishx%,kdely%,kdelx%,lne$,kcolr%)
DECLARE sub READSET()
DECLARE sub GETSETUP()
'DECLARE sub getscratio()
DECLARE function lftvar$(strgvar$)  'returns leftmost variable from strgvar$; strgvar$ is shortened

'following defined and used in MT5MEN2
'DECLARE SUB eaproj (irad%, toang!, az!, x!, y!)

'following are defined in M5MTSUB
'DECLARE SUB AMPLSH ()
DECLARE SUB eaproj2 (x0!, y0!, rad0!, ang!, az!, x!, y!)
DECLARE SUB getplotnm (gsin$, gsf$, gss$)
DECLARE SUB linetype (nsor%, kline%, ds!)
'DECLARE SUB plotnow (kline%, pincr!, kcol%)
DECLARE SUB PLOTPMTRAD (x0!, y0!, prad!)
DECLARE SUB PLOTPRAD (x0!, y0!, prad!)
DECLARE SUB PLOTSMTRAD (x0!, y0!, prad!)
DECLARE SUB PLOTSRAD (x0!, y0!, prad!)
'DECLARE SUB PROJNET (x0!, y0!, prad!)
'DECLARE SUB scrscale (fisx!, fisy!)

'following are defined in M5GRSUBS
DECLARE SUB clnpar (s$)
'DECLARE SUB gsappend (f$)
DECLARE SUB gscircle (x!, y!, rd!, dthet!, kcol%)
DECLARE SUB gsclose (f$)
DECLARE SUB gsdot (x!, y!)
DECLARE SUB gsdraw (x!, y!, kcolor%)
DECLARE SUB gsinit (f$)
DECLARE SUB gsline (x!(), y!(), n%, lsyn%, sincr!, kcolor%)
DECLARE SUB gsmove (x!, y!, kcolor%)
DECLARE SUB gsopen (f$)
DECLARE SUB gsorig (x!, y!)
DECLARE SUB gssym (kfill%, x!, y!, ksymb%, an!, sz!)
DECLARE SUB gstext (n%, x!, y!, t$, an!, sz!)
DECLARE SUB gstroke ()
'DECLARE SUB hpcmd (x1!, y1!, icode%, kcolor%)
'DECLARE SUB hpcnvrt (x1!, y1!)
'DECLARE SUB hpcolor (kcolor%)
'DECLARE SUB hptext (xx!, yy!, size!, ang!, t$)
DECLARE SUB scletter (ix%, iy%, c$, iangin%, size!, isize%)

'Following are defined in M5EIGEN
DECLARE SUB mtiso(sorpar!(2),isor%,amdev!,amiso!)
DECLARE SUB mtadj(sorpar!(2),isor%,tam!(1),ihld%)
DECLARE SUB mteigen(isdblcpl%,sorpar!(2),isor%,tam!(1),tamiso!(1),ierr%)
